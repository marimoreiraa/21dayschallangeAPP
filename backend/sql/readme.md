### /sql - Contém a estrutura para criação do banco de dados MySql

Para criar a base e as tabelas no banco em localhost, basta abrir um terminal na pasta principal e executar ``npm run database-setup``